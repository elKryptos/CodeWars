ArrayList
	Crea una ArrayList di Stringhe e aggiungi alcuni elementi.
	
	Rimuovi un elemento specifico da una ArrayList.//remove
	
	Recupera un elemento specifico dalla ArrayList.//get
	
	Verifica se un elemento esiste nella ArrayList.//contains
	
	Trova la dimensione della ArrayList.//size
	
	Itera attraverso tutti gli elementi della ArrayList usando un ciclo for.
	
	Converti una ArrayList in un array. // Object [] array = x.toArray()  -->  Arrays.toString(array)
	
	Trova l'indice di un elemento specifico nella ArrayList. //get
	
	Ordina gli elementi della ArrayList. //Collections.sort(x)
	
	Copia tutti gli elementi di una ArrayList in un'altra ArrayList.//addAll()
	
	Svuota una ArrayList.  //clear
	
	Aggiungi tutti gli elementi di una ArrayList a un'altra ArrayList.
	
	Clona una ArrayList.//
	
	Sostituisci un elemento specifico in una ArrayList. //set(index, "")
	
	Aggiungi un elemento in una posizione specifica nella ArrayList.// add(index, "")
	
	Usa un iteratore per scorrere la ArrayList. List<String> iter = x.iterator();  while(iter.hasNext()){syout(iter.next())}
	
	Usa i metodi subList e clear su una ArrayList. //subList(fromIndex, toIndex)     clear()
	
	Converti una ArrayList in una lista non modificabile.
	
	Filtra gli elementi di una ArrayList usando una funzione lambda.
	
	Esegui operazioni di fusione e differenza tra due ArrayList.

HashSet
	Crea un HashSet di numeri interi e aggiungi alcuni elementi.
	
	Rimuovi un elemento specifico da un HashSet.
	
	Verifica se un elemento esiste in un HashSet.
	
	Trova la dimensione di un HashSet.
	
	Itera attraverso tutti gli elementi di un HashSet usando un ciclo for-each.
	
	Converti un HashSet in un array.
	
	Trova l'intersezione di due HashSet.
	
	Trova l'unione di due HashSet.
	
	Trova la differenza tra due HashSet.
	
	Verifica se un HashSet è un sottoinsieme di un altro HashSet.
	
	Usa un iteratore per scorrere un HashSet.
	
	Aggiungi tutti gli elementi di un HashSet a un altro HashSet.
	
	Svuota un HashSet.
	
	Copia tutti gli elementi di un HashSet in un altro HashSet.
	
	Rimuovi tutti gli elementi che soddisfano una certa condizione da un HashSet.
	
	Converti un HashSet in una lista non modificabile.
	
	Usa il metodo stream() su un HashSet per operazioni funzionali.
	
	Filtra gli elementi di un HashSet usando una funzione lambda.
	
	Trova elementi duplicati in una lista usando un HashSet.
	
	Usa un HashSet per trovare la frequenza degli elementi in una lista.

HashMap
	Crea una HashMap di Stringhe e Interi e aggiungi alcune coppie chiave-valore.
	
	Rimuovi una coppia chiave-valore da una HashMap.
	
	Recupera un valore specifico da una HashMap usando la chiave.
	
	Verifica se una chiave esiste nella HashMap.
	
	Verifica se un valore esiste nella HashMap.
	
	Trova la dimensione di una HashMap.
	
	Itera attraverso tutte le chiavi di una HashMap.
	
	Itera attraverso tutti i valori di una HashMap.
	
	Itera attraverso tutte le coppie chiave-valore di una HashMap.
	
	Trova la chiave con il valore più alto in una HashMap.